{% extends "base_auth.html.twig" %}

{% block body %}
    {% block head %}
        <link rel="stylesheet" type="text/css" href="styles/forgot.css">
    {% endblock %}
    
    <section class="gradient-form">
        <div class="container-fluid h-100 d-flex justify-content-center align-items-center">
            <div class="card">
                <div class="card-body">
                    <h1 class="text-center mb-5">Mot de passe oublié</h1>
                    {% if msg %}
                        <div class="alert alert-info">{{ msg }}</div>
                    {% endif %}
                    <form action="" method="post">
                        <div class="form-group">
                            <label for="email" class="form-label">Email :</label>
                            <input type="email" class="form-control" id="email" name="email" required>
                        </div>
                        <div class="form-group">
                            <label for="new_password" class="form-label">Nouveau mot de passe :</label>
                            <input type="password" class="form-control" id="new_password" name="new_password" required>
                        </div>
                        <div class="form-group">
                            <label for="confirm_password" class="form-label">Confirmez le mot de passe :</label>
                            <input type="password" class="form-control" id="confirm_password" name="confirm_password" required>
                        </div>
                        <div class="text-center">
                            <button type="submit" name="send" class="btn btn-success btn-block">Réinitialiser le mot de passe</button>
                        </div>
                        <div class="return-link">
                            <a href="index.php">Retour</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
